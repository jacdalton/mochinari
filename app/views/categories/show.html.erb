<div class="container">
  <div class="col">
    <h2>
      <%= @category.name %>
    </h2>
    <p>
      <%= @category.description  %>
    </p>
    <p>tag tag tag tag</p>
    <%= link_to "Back", categories_path %>
    <h3>Examples of <%= @category.name %></h3>
    <div class="row">
      <% @category.snacks.each do |snack| %>
        <div class="snack-index-card">
          <%= image_tag snack.snack_images.sample.image_path %>
          <div class="snack-index-card-infos">
            <div>
              <h3 class="info"> <%= snack.name %> </h3>
              <div class="stars">
                <% (snack.avg_snack_stars).times do %>
                  <i class="fas fa-star"></i>
                <% end %>
                <% (1 - snack.avg_snack_stars).times do %>
                  <i class="far fa-star"></i>
                <% end %>
              </div>
            </div>
            <%= image_tag("bobby", :alt=> "bananadeer", class: "bobby") %>
          </div>
        </div>
      <% end %>
    </div>
  </div>