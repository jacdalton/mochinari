var value = '<%= j @value %>'
var snack_id = '<%= j @snack.id.to_s %>'

function toggleEdit() {
 const button = document.querySelector(`#toggle-button-${snack_id}`);
  if (value === "added") {
    button.innerHTML = '';
  }
}

toggleEdit();
