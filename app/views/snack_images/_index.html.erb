
  <div class="mochies-card d-flex" >
    <% snack_images.each do |img| %>
      <%= cl_image_tag img.image_path, class: "mochies-card col-xs-3 col-sm-3 col-md-3 col-lg-3 mt-2" %>
    <% end %>
  </div>
